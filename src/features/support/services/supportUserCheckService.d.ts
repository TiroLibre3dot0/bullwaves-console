// Minimal TypeScript sidecar declarations for `supportUserCheckService.js`.
// Keep this file lean and extend as needed.

export function loadCsvRows(force?: boolean): Promise<any[]>

export function computeActivityIntelligence(row: any, now?: Date): any
